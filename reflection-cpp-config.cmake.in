@PACKAGE_INIT@

# prevent repeatedly including the targets
if(NOT TARGET reflection-cpp::reflection-cpp)
    include(${CMAKE_CURRENT_LIST_DIR}/reflection-cpp-targets.cmake)
endif()

message(STATUS "Found @PROJECT_NAME@, version: ${@PROJECT_NAME@_VERSION}")
